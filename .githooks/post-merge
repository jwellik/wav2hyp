#!/usr/bin/env bash
# After merge: refresh branch timeline (close-out of merged branch is manual in summary files).
set -e
cd "$(git rev-parse --show-toplevel)"
python3 scripts/branch_docs.py regenerate-timeline
